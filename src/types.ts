
// Type Normal
export type TVideo = {
    id: string,
    title: string,
    durationSecond: number,
    dateUpload: string
}

// Type Banco de Dados
export type TVideoDB = {
    id: string,
    title: string,
    duration_second: number,
    date_upload: string
}